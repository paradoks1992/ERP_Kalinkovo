{
  "name": "erp_kalinkovo_frontend",
  "private": true,
  "packageManager": "pnpm@10.18.3",
  "scripts": {
    "dev:monitor": "pnpm --filter @apps/monitor dev",
    "build:monitor": "pnpm --filter @apps/monitor build",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
